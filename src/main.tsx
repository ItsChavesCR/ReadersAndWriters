import React from 'react'
import ReactDOM from 'react-dom/client'
import { ReadersAndWriters } from './ReadersAndWriters'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ReadersAndWriters />
  </React.StrictMode>,
)
